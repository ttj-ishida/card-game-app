# M0 詳細TODO：開発土台とカードカタログ

- 文書ID：GAME-TODO-DETAIL-M0
- 版数：0.2
- 基準文書：`独自カードゲーム_横断マイルストーン_TODO_v0.1.md`、`独自カードゲーム_要件定義書_v0.1.md`
- TODO件数：18件

## このマイルストーンの到達状態

開発環境でアプリを起動し、全42枚を仮素材付きで確認できる。

## 確定前提

- v1.0はAndroid先行。iOSと公式対戦は将来版とする。
- ゲスト開始後、Googleまたはメールへアカウント連携できる構成とする。
- 表示名・用語・カード名と内部IDを分離し、将来の英語化に備える。

## 共通実施ルール

- 依存TODOが未完了の場合は着手しない。並行可能な場合は、共有インターフェースだけ先に固定する。
- 表示名・日本語文言を内部IDや対局状態へ保存しない。安定IDと言語リソースを使用する。
- 非公開手札、未使用スキル、認証情報、個人情報をクライアントログや分析イベントへ出さない。
- Supabase変更はマイグレーション、Expo変更は自動テスト、グラフィック変更はアセット対応表を必須とする。
- 曖昧さや確定仕様との矛盾を発見した場合は作業を止め、要件の決定者へ確認する。

## TODO一覧

| TODO ID | 領域 | 概要 | 依存 |
|---|---|---|---|
| M0-PM-01 | 横断 | リポジトリ構成、ブランチ方針、命名規則を決める | なし |
| M0-PM-02 | 横断 | 開発・検証・本番の環境分離方針を決める | M0-PM-01 |
| M0-SB-01 | SB | Supabase開発プロジェクトを作成する | M0-PM-02 |
| M0-SB-02 | SB | マイグレーション実行基盤を用意する | M0-SB-01 |
| M0-SB-03 | SB | `rulesets`、`terms`、`number_cards`、`skill_cards`のマスタを作る | M0-SB-02 |
| M0-SB-04 | SB | 数字36枚、スキル6枚、属性4種のシードを作る | M0-SB-03 |
| M0-SB-05 | SB | 公開マスタの読取権限と管理更新権限を分離する | M0-SB-03 |
| M0-EX-01 | EX | Expo＋TypeScriptのプロジェクトを作成する | M0-PM-01 |
| M0-EX-02 | EX | 環境変数と開発・検証・本番の切替を用意する | M0-EX-01、M0-PM-02 |
| M0-EX-03 | EX | 画面遷移、状態管理、テスト、Lint、Formatterを導入する | M0-EX-01 |
| M0-EX-04 | EX | 言語リソースキーと日本語辞書の土台を作る | M0-EX-01 |
| M0-EX-05 | EX | デザイントークンを作る | M0-GR-01 |
| M0-GR-01 | GR | アートディレクション1枚資料を作る | なし |
| M0-GR-02 | GR | カード縦横比、セーフエリア、書出しサイズを決める | M0-GR-01 |
| M0-GR-03 | GR | 火・水・風・土の仮紋章と昼夜パレットを作る | M0-GR-01 |
| M0-GR-04 | GR | 数字カード、スキル、カード裏面の仮素材を作る | M0-GR-02、M0-GR-03 |
| M0-QA-01 | QA | カードカタログ画面でSupabaseマスタと仮素材を結合する | M0-SB-04、M0-EX-04、M0-GR-04 |
| M0-QA-02 | QA | 新規セットアップ手順を別ディレクトリで再現する | M0-SB-02、M0-EX-03 |

---

## M0-PM-01：リポジトリ構成、ブランチ方針、命名規則を決める

- 領域：横断
- 依存TODO：なし
- 目的：リポジトリ構成、ブランチ方針、命名規則を決めることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「リポジトリ構成、ブランチ方針、命名規則を決める」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M0-PM-01` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- READMEから構成を説明できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：READMEから構成を説明できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-PM-02：開発・検証・本番の環境分離方針を決める

- 領域：横断
- 依存TODO：M0-PM-01
- 目的：開発・検証・本番の環境分離方針を決めることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「開発・検証・本番の環境分離方針を決める」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M0-PM-02` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- 環境名と用途が文書化される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：環境名と用途が文書化される。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-SB-01：Supabase開発プロジェクトを作成する

- 領域：SB
- 依存TODO：M0-PM-02
- 目的：Supabase開発プロジェクトを作成することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Supabase開発プロジェクトを作成する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 開発用接続情報で疎通できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：開発用接続情報で疎通できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-SB-02：マイグレーション実行基盤を用意する

- 領域：SB
- 依存TODO：M0-SB-01
- 目的：マイグレーション実行基盤を用意することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「マイグレーション実行基盤を用意する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 空DBから同じ構造を再現できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：空DBから同じ構造を再現できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-SB-03：`rulesets`、`terms`、`number_cards`、`skill_cards`のマスタを作る

- 領域：SB
- 依存TODO：M0-SB-02
- 目的：`rulesets`、`terms`、`number_cards`、`skill_cards`のマスタを作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`rulesets`、`terms`、`number_cards`、`skill_cards`のマスタを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 全マスタをSQLで再作成できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：全マスタをSQLで再作成できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-SB-04：数字36枚、スキル6枚、属性4種のシードを作る

- 領域：SB
- 依存TODO：M0-SB-03
- 目的：数字36枚、スキル6枚、属性4種のシードを作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「数字36枚、スキル6枚、属性4種のシードを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 件数・一意性検査が成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：件数・一意性検査が成功する。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-SB-05：公開マスタの読取権限と管理更新権限を分離する

- 領域：SB
- 依存TODO：M0-SB-03
- 目的：公開マスタの読取権限と管理更新権限を分離することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公開マスタの読取権限と管理更新権限を分離する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 一般クライアントが直接更新できない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：一般クライアントが直接更新できない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-EX-01：Expo＋TypeScriptのプロジェクトを作成する

- 領域：EX
- 依存TODO：M0-PM-01
- 目的：Expo＋TypeScriptのプロジェクトを作成することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Expo＋TypeScriptのプロジェクトを作成する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- AndroidエミュレーターとAndroid実機で初期画面が開く。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：AndroidエミュレーターとAndroid実機で初期画面が開く。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-EX-02：環境変数と開発・検証・本番の切替を用意する

- 領域：EX
- 依存TODO：M0-EX-01、M0-PM-02
- 目的：環境変数と開発・検証・本番の切替を用意することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「環境変数と開発・検証・本番の切替を用意する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- ビルドごとに接続先を切り替えられる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：ビルドごとに接続先を切り替えられる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-EX-03：画面遷移、状態管理、テスト、Lint、Formatterを導入する

- 領域：EX
- 依存TODO：M0-EX-01
- 目的：画面遷移、状態管理、テスト、Lint、Formatterを導入することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「画面遷移、状態管理、テスト、Lint、Formatterを導入する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- CIまたはローカル一括検査が成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：CIまたはローカル一括検査が成功する。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-EX-04：言語リソースキーと日本語辞書の土台を作る

- 領域：EX
- 依存TODO：M0-EX-01
- 目的：言語リソースキーと日本語辞書の土台を作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「言語リソースキーと日本語辞書の土台を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 表示名をコード変更なしで差し替えられる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：表示名をコード変更なしで差し替えられる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-EX-05：デザイントークンを作る

- 領域：EX
- 依存TODO：M0-GR-01
- 目的：デザイントークンを作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「デザイントークンを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 色、余白、文字、角丸を共通参照できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：色、余白、文字、角丸を共通参照できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-GR-01：アートディレクション1枚資料を作る

- 領域：GR
- 依存TODO：なし
- 目的：アートディレクション1枚資料を作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「アートディレクション1枚資料を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 世界観、色、線、質感、禁止例が確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：世界観、色、線、質感、禁止例が確認できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-GR-02：カード縦横比、セーフエリア、書出しサイズを決める

- 領域：GR
- 依存TODO：M0-GR-01
- 目的：カード縦横比、セーフエリア、書出しサイズを決めることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「カード縦横比、セーフエリア、書出しサイズを決める」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M0-GR-02` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- テンプレートが作成される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：テンプレートが作成される。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-GR-03：火・水・風・土の仮紋章と昼夜パレットを作る

- 領域：GR
- 依存TODO：M0-GR-01
- 目的：火・水・風・土の仮紋章と昼夜パレットを作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「火・水・風・土の仮紋章と昼夜パレットを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 色なしでも4属性を識別できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：色なしでも4属性を識別できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-GR-04：数字カード、スキル、カード裏面の仮素材を作る

- 領域：GR
- 依存TODO：M0-GR-02、M0-GR-03
- 目的：数字カード、スキル、カード裏面の仮素材を作ることで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「数字カード、スキル、カード裏面の仮素材を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 全カードIDに対応する画像がある。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：全カードIDに対応する画像がある。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-QA-01：カードカタログ画面でSupabaseマスタと仮素材を結合する

- 領域：QA
- 依存TODO：M0-SB-04、M0-EX-04、M0-GR-04
- 目的：カードカタログ画面でSupabaseマスタと仮素材を結合することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「カードカタログ画面でSupabaseマスタと仮素材を結合する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 42枚を欠落なく一覧・拡大表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：42枚を欠落なく一覧・拡大表示できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M0-QA-02：新規セットアップ手順を別ディレクトリで再現する

- 領域：QA
- 依存TODO：M0-SB-02、M0-EX-03
- 目的：新規セットアップ手順を別ディレクトリで再現することで、開発土台とカードカタログの到達状態である「開発環境でアプリを起動し、全42枚を仮素材付きで確認できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「新規セットアップ手順を別ディレクトリで再現する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 手順だけでAndroidアプリ起動とDBマイグレーションを再現できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：手順だけでAndroidアプリ起動とDBマイグレーションを再現できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。
