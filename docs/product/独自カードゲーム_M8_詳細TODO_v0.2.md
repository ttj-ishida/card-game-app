# M8 詳細TODO：Androidストア公開版

- 文書ID：GAME-TODO-DETAIL-M8
- 版数：0.2
- 基準文書：`独自カードゲーム_横断マイルストーン_TODO_v0.1.md`、`独自カードゲーム_要件定義書_v0.1.md`
- TODO件数：19件

## このマイルストーンの到達状態

CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる。

## 確定前提

- v1.0はCPU＋フレンド、Android、日本語のみ、全世界配信、13歳以上とする。
- 同時接続100人、月間可用性99.0%、通常操作の95%を2秒以内、クラッシュなしセッション99.0%以上を目標とする。
- RPO 24時間、RTO 8時間、ログ保持90日、外部サービス月額上限1万円を運用基準とする。

## 共通実施ルール

- 依存TODOが未完了の場合は着手しない。並行可能な場合は、共有インターフェースだけ先に固定する。
- 表示名・日本語文言を内部IDや対局状態へ保存しない。安定IDと言語リソースを使用する。
- 非公開手札、未使用スキル、認証情報、個人情報をクライアントログや分析イベントへ出さない。
- Supabase変更はマイグレーション、Expo変更は自動テスト、グラフィック変更はアセット対応表を必須とする。
- 曖昧さや確定仕様との矛盾を発見した場合は作業を止め、要件の決定者へ確認する。

## TODO一覧

| TODO ID | 領域 | 概要 | 依存 |
|---|---|---|---|
| M8-PM-01 | 横断 | Android v1.0のCPU＋フレンド範囲と対象外を最終確定する | M7-QA-03 |
| M8-PM-02 | 横断 | Android 10以上、全世界、13歳以上、無料＋広告を確定する | M8-PM-01 |
| M8-SB-01 | SB | 本番Supabaseプロジェクトと秘密情報を分離する | M7-SB-05 |
| M8-SB-02 | SB | 本番マイグレーションとシードを実行・検証する | M8-SB-01 |
| M8-SB-03 | SB | バックアップ、RPO、RTO、復元手順を確定する | M8-SB-02 |
| M8-SB-04 | SB | 本番監視、アラート、フレンド対戦受付停止を検証する | M7-SB-04、05 |
| M8-SB-05 | SB | 本番負荷を想定した同時対局テストを行う | M8-SB-02 |
| M8-EX-01 | EX | 本番ビルド設定、署名、環境変数を確定する | M8-SB-01 |
| M8-EX-02 | EX | Androidスマホ・タブレット実機の全E2Eを実行する | M8-EX-01 |
| M8-EX-03 | EX | Google Play審査用アカウントと確認手順を用意する | M8-EX-02 |
| M8-EX-04 | EX | クラッシュ、ANR、メモリ、通信失敗を最終確認する | M8-EX-02 |
| M8-EX-05 | EX | Google Play段階公開ビルドを作成・提出する | M8-EX-02～04 |
| M8-GR-01 | GR | Android用アプリアイコン、起動画面、Google Play画像を完成する | M7-GR-02 |
| M8-GR-02 | GR | ストア説明用の対局スクリーンショットを作る | M8-EX-02 |
| M8-GR-03 | GR | 全画面の仮素材・欠落・誤表記を最終確認する | M6-QA-01 |
| M8-QA-01 | QA | 要件トレーサビリティを完成する | 全マイルストーン |
| M8-QA-02 | QA | CPU・フレンドの回帰、切断、競合、情報漏洩、広告失敗を再実行する | M8-QA-01 |
| M8-QA-03 | QA | 少人数の本番相当ベータを実施する | M8-QA-02 |
| M8-QA-04 | QA | 段階公開の継続・停止判断基準を作る | M8-QA-03 |

---

## M8-PM-01：Android v1.0のCPU＋フレンド範囲と対象外を最終確定する

- 領域：横断
- 依存TODO：M7-QA-03
- 目的：Android v1.0のCPU＋フレンド範囲と対象外を最終確定することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Android v1.0のCPU＋フレンド範囲と対象外を最終確定する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M8-PM-01` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- 公式対戦、iOS、英語がv1.0対象外として凍結される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：公式対戦、iOS、英語がv1.0対象外として凍結される。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- v1.0のCPUは標準1種類。画面上の思考待ちは0.6〜1.2秒とする。

---

## M8-PM-02：Android 10以上、全世界、13歳以上、無料＋広告を確定する

- 領域：横断
- 依存TODO：M8-PM-01
- 目的：Android 10以上、全世界、13歳以上、無料＋広告を確定することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Android 10以上、全世界、13歳以上、無料＋広告を確定する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M8-PM-02` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- Google Play入力情報が確定する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：Google Play入力情報が確定する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。

---

## M8-SB-01：本番Supabaseプロジェクトと秘密情報を分離する

- 領域：SB
- 依存TODO：M7-SB-05
- 目的：本番Supabaseプロジェクトと秘密情報を分離することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「本番Supabaseプロジェクトと秘密情報を分離する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 開発者端末に本番秘密鍵を置かない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：開発者端末に本番秘密鍵を置かない。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-SB-02：本番マイグレーションとシードを実行・検証する

- 領域：SB
- 依存TODO：M8-SB-01
- 目的：本番マイグレーションとシードを実行・検証することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「本番マイグレーションとシードを実行・検証する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 空本番環境を再現できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：空本番環境を再現できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-SB-03：バックアップ、RPO、RTO、復元手順を確定する

- 領域：SB
- 依存TODO：M8-SB-02
- 目的：バックアップ、RPO、RTO、復元手順を確定することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「バックアップ、RPO、RTO、復元手順を確定する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M8-SB-03` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- 復元演習が成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：復元演習が成功する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-SB-04：本番監視、アラート、フレンド対戦受付停止を検証する

- 領域：SB
- 依存TODO：M7-SB-04、05
- 目的：本番監視、アラート、フレンド対戦受付停止を検証することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「本番監視、アラート、フレンド対戦受付停止を検証する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- テスト障害で通知と新規受付停止ができる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：テスト障害で通知と新規受付停止ができる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-SB-05：本番負荷を想定した同時対局テストを行う

- 領域：SB
- 依存TODO：M8-SB-02
- 目的：本番負荷を想定した同時対局テストを行うことで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「本番負荷を想定した同時対局テストを行う」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 確定した性能目標を満たす。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：確定した性能目標を満たす。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-EX-01：本番ビルド設定、署名、環境変数を確定する

- 領域：EX
- 依存TODO：M8-SB-01
- 目的：本番ビルド設定、署名、環境変数を確定することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「本番ビルド設定、署名、環境変数を確定する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M8-EX-01` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- 開発・検証・本番を取り違えない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：開発・検証・本番を取り違えない。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-EX-02：Androidスマホ・タブレット実機の全E2Eを実行する

- 領域：EX
- 依存TODO：M8-EX-01
- 目的：Androidスマホ・タブレット実機の全E2Eを実行することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Androidスマホ・タブレット実機の全E2Eを実行する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- CPU、フレンド、広告、退会がAndroid実機で完走する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：CPU、フレンド、広告、退会がAndroid実機で完走する。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-EX-03：Google Play審査用アカウントと確認手順を用意する

- 領域：EX
- 依存TODO：M8-EX-02
- 目的：Google Play審査用アカウントと確認手順を用意することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Google Play審査用アカウントと確認手順を用意する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 審査担当が主要機能を確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：審査担当が主要機能を確認できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-EX-04：クラッシュ、ANR、メモリ、通信失敗を最終確認する

- 領域：EX
- 依存TODO：M8-EX-02
- 目的：クラッシュ、ANR、メモリ、通信失敗を最終確認することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「クラッシュ、ANR、メモリ、通信失敗を最終確認する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 確定した品質目標を満たす。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：確定した品質目標を満たす。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-EX-05：Google Play段階公開ビルドを作成・提出する

- 領域：EX
- 依存TODO：M8-EX-02～04
- 目的：Google Play段階公開ビルドを作成・提出することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Google Play段階公開ビルドを作成・提出する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 対象環境、版番号、ビルド設定、署名、接続先、秘密情報、配布対象をチェックリストで固定する。
3. クリーン環境から同一成果物を再生成し、インストール、起動、更新、ロールバックまたは配布停止を確認する。
4. 既知制約、確認手順、問い合わせ時に必要な情報をリリースノートへ記載する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 再現可能なビルドまたは配布物
- チェックリスト
- リリースノート

### 完了条件

- ストア審査へ提出できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：ストア審査へ提出できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-GR-01：Android用アプリアイコン、起動画面、Google Play画像を完成する

- 領域：GR
- 依存TODO：M7-GR-02
- 目的：Android用アプリアイコン、起動画面、Google Play画像を完成することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Android用アプリアイコン、起動画面、Google Play画像を完成する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- Google Playの必要サイズが揃う。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：Google Playの必要サイズが揃う。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-GR-02：ストア説明用の対局スクリーンショットを作る

- 領域：GR
- 依存TODO：M8-EX-02
- 目的：ストア説明用の対局スクリーンショットを作ることで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ストア説明用の対局スクリーンショットを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 実際のゲーム内容と一致する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：実際のゲーム内容と一致する。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-GR-03：全画面の仮素材・欠落・誤表記を最終確認する

- 領域：GR
- 依存TODO：M6-QA-01
- 目的：全画面の仮素材・欠落・誤表記を最終確認することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「全画面の仮素材・欠落・誤表記を最終確認する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 仮素材0件、カード対応ミス0件。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：仮素材0件、カード対応ミス0件。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-QA-01：要件トレーサビリティを完成する

- 領域：QA
- 依存TODO：全マイルストーン
- 目的：要件トレーサビリティを完成することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「要件トレーサビリティを完成する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- Must要件がテストへ紐づく。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：Must要件がテストへ紐づく。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-QA-02：CPU・フレンドの回帰、切断、競合、情報漏洩、広告失敗を再実行する

- 領域：QA
- 依存TODO：M8-QA-01
- 目的：CPU・フレンドの回帰、切断、競合、情報漏洩、広告失敗を再実行することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「CPU・フレンドの回帰、切断、競合、情報漏洩、広告失敗を再実行する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- リリース阻害不具合が0件。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：リリース阻害不具合が0件。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。
- v1.0のCPUは標準1種類。画面上の思考待ちは0.6〜1.2秒とする。

---

## M8-QA-03：少人数の本番相当ベータを実施する

- 領域：QA
- 依存TODO：M8-QA-02
- 目的：少人数の本番相当ベータを実施することで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「少人数の本番相当ベータを実施する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 結果・監視・問合せ対応を確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：結果・監視・問合せ対応を確認できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M8-QA-04：段階公開の継続・停止判断基準を作る

- 領域：QA
- 依存TODO：M8-QA-03
- 目的：段階公開の継続・停止判断基準を作ることで、Androidストア公開版の到達状態である「CPU＋フレンド対戦の日本語Android版を全世界・13歳以上向けに公開できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「段階公開の継続・停止判断基準を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 指標から公開率を判断できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：指標から公開率を判断できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。
