# M5 詳細TODO：将来版：人間4人の公式対戦

- 文書ID：GAME-TODO-DETAIL-M5
- 版数：0.2
- 基準文書：`独自カードゲーム_横断マイルストーン_TODO_v0.1.md`、`独自カードゲーム_要件定義書_v0.1.md`
- TODO件数：28件

## このマイルストーンの到達状態

v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する。

## 確定前提

- 本マイルストーンはv1.0のリリース条件に含めない。M8を阻害しない独立バックログとする。
- 公式対戦を実装する将来版では人間4人固定、CPU補充なしを維持する。

## 共通実施ルール

- 依存TODOが未完了の場合は着手しない。並行可能な場合は、共有インターフェースだけ先に固定する。
- 表示名・日本語文言を内部IDや対局状態へ保存しない。安定IDと言語リソースを使用する。
- 非公開手札、未使用スキル、認証情報、個人情報をクライアントログや分析イベントへ出さない。
- Supabase変更はマイグレーション、Expo変更は自動テスト、グラフィック変更はアセット対応表を必須とする。
- 曖昧さや確定仕様との矛盾を発見した場合は作業を止め、要件の決定者へ確認する。

## TODO一覧

| TODO ID | 領域 | 概要 | 依存 |
|---|---|---|---|
| M5-SB-01 | SB | 公式マッチングキューを作る | M4-SB-04 |
| M5-SB-02 | SB | マッチング取消・期限切れ処理を作る | M5-SB-01 |
| M5-SB-03 | SB | 人間4人確認後の公式対局作成を作る | M5-SB-01、M4-SB-05 |
| M5-SB-04 | SB | サーバー基準の手番期限を保存する | M4-SB-03 |
| M5-SB-05 | SB | 時間切れ確定APIを作る | M5-SB-04 |
| M5-SB-06 | SB | 連続時間切れ、60秒切断、明示退出の棄権処理を作る | M5-SB-05、M5-SB-12 |
| M5-SB-07 | SB | `official_round_results`と公式勝利数を作る | M5-SB-03 |
| M5-SB-08 | SB | 結果保存と勝利数加算を冪等・原子的にする | M5-SB-07 |
| M5-SB-09 | SB | 公式順位ビューを作る | M5-SB-07 |
| M5-SB-10 | SB | 対局監査ログと不審操作ログを作る | M4-SB-06 |
| M5-SB-11 | SB | モード別統計の分離テストを作る | M5-SB-07 |
| M5-SB-12 | SB | 接続状態、最終応答時刻、再接続猶予の管理を作る | M4-SB-10 |
| M5-EX-01 | EX | 公式マッチング画面と取消を作る | M5-SB-01、02 |
| M5-EX-02 | EX | 長時間待機時の継続・カジュアル案内を作る | M5-EX-01 |
| M5-EX-03 | EX | 30秒タイマーと残り10秒警告を作る | M5-SB-04 |
| M5-EX-04 | EX | 時間切れ時の自動パス・自動提出表示を作る | M5-SB-05 |
| M5-EX-05 | EX | 切断猶予、棄権、残り1人勝利の表示を作る | M5-SB-06 |
| M5-EX-06 | EX | 公式結果・公式勝利数画面を作る | M5-SB-07、08 |
| M5-EX-07 | EX | 公式順位画面を作る | M5-SB-09 |
| M5-EX-08 | EX | 公式対戦プロフィール識別を作る | M5-SB-07 |
| M5-GR-01 | GR | 公式対戦、マッチング、順位の視覚体系を作る | M4-GR-01 |
| M5-GR-02 | GR | 30秒タイマー・10秒警告・棄権の演出素材を作る | M5-EX-03～05 |
| M5-GR-03 | GR | 公式勝利結果と順位表示素材を作る | M5-EX-06、07 |
| M5-QA-01 | QA | マッチング同時参加・取消競合をテストする | M5-SB-01～03 |
| M5-QA-02 | QA | タイマー境界29.9秒・30秒・再送をテストする | M5-SB-04、05 |
| M5-QA-03 | QA | 切断59秒・61秒・3回時間切れをテストする | M5-SB-06 |
| M5-QA-04 | QA | 結果・勝利数の同時更新と再送をテストする | M5-SB-08 |
| M5-QA-05 | QA | 招待テスターで100局実施する | M5-QA-01～04 |

---

## M5-SB-01：公式マッチングキューを作る

- 領域：SB
- 依存TODO：M4-SB-04
- 目的：公式マッチングキューを作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式マッチングキューを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 4人を重複なく1組へ割り当てられる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：4人を重複なく1組へ割り当てられる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-02：マッチング取消・期限切れ処理を作る

- 領域：SB
- 依存TODO：M5-SB-01
- 目的：マッチング取消・期限切れ処理を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「マッチング取消・期限切れ処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 取消済みユーザーが対局へ入らない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：取消済みユーザーが対局へ入らない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-03：人間4人確認後の公式対局作成を作る

- 領域：SB
- 依存TODO：M5-SB-01、M4-SB-05
- 目的：人間4人確認後の公式対局作成を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「人間4人確認後の公式対局作成を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- CPU席なしで配布される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：CPU席なしで配布される。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-04：サーバー基準の手番期限を保存する

- 領域：SB
- 依存TODO：M4-SB-03
- 目的：サーバー基準の手番期限を保存することで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「サーバー基準の手番期限を保存する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 全端末で同じ期限を参照できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：全端末で同じ期限を参照できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-05：時間切れ確定APIを作る

- 領域：SB
- 依存TODO：M5-SB-04
- 目的：時間切れ確定APIを作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「時間切れ確定APIを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 誰が呼び出しても期限前は拒否し、期限後は1回だけ自動処理される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：誰が呼び出しても期限前は拒否し、期限後は1回だけ自動処理される。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-06：連続時間切れ、60秒切断、明示退出の棄権処理を作る

- 領域：SB
- 依存TODO：M5-SB-05、M5-SB-12
- 目的：連続時間切れ、60秒切断、明示退出の棄権処理を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「連続時間切れ、60秒切断、明示退出の棄権処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 残り手札除外と手番スキップが原子的に行われる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：残り手札除外と手番スキップが原子的に行われる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-07：`official_round_results`と公式勝利数を作る

- 領域：SB
- 依存TODO：M5-SB-03
- 目的：`official_round_results`と公式勝利数を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`official_round_results`と公式勝利数を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 勝者だけに1勝を加算できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：勝者だけに1勝を加算できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-08：結果保存と勝利数加算を冪等・原子的にする

- 領域：SB
- 依存TODO：M5-SB-07
- 目的：結果保存と勝利数加算を冪等・原子的にすることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「結果保存と勝利数加算を冪等・原子的にする」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 再送しても勝利数が二重加算されない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：再送しても勝利数が二重加算されない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-09：公式順位ビューを作る

- 領域：SB
- 依存TODO：M5-SB-07
- 目的：公式順位ビューを作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式順位ビューを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 勝利数順、同勝利数同順位になる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：勝利数順、同勝利数同順位になる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-10：対局監査ログと不審操作ログを作る

- 領域：SB
- 依存TODO：M4-SB-06
- 目的：対局監査ログと不審操作ログを作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「対局監査ログと不審操作ログを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 対局IDから要求と結果を追跡できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：対局IDから要求と結果を追跡できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-11：モード別統計の分離テストを作る

- 領域：SB
- 依存TODO：M5-SB-07
- 目的：モード別統計の分離テストを作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「モード別統計の分離テストを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- CPU・フレンド結果が公式へ入らない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：CPU・フレンド結果が公式へ入らない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-SB-12：接続状態、最終応答時刻、再接続猶予の管理を作る

- 領域：SB
- 依存TODO：M4-SB-10
- 目的：接続状態、最終応答時刻、再接続猶予の管理を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「接続状態、最終応答時刻、再接続猶予の管理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 60秒境界をサーバー時刻で判定できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：60秒境界をサーバー時刻で判定できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-01：公式マッチング画面と取消を作る

- 領域：EX
- 依存TODO：M5-SB-01、02
- 目的：公式マッチング画面と取消を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式マッチング画面と取消を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 待機時間と状態を表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：待機時間と状態を表示できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-02：長時間待機時の継続・カジュアル案内を作る

- 領域：EX
- 依存TODO：M5-EX-01
- 目的：長時間待機時の継続・カジュアル案内を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「長時間待機時の継続・カジュアル案内を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- ユーザーが選択して遷移できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：ユーザーが選択して遷移できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-03：30秒タイマーと残り10秒警告を作る

- 領域：EX
- 依存TODO：M5-SB-04
- 目的：30秒タイマーと残り10秒警告を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「30秒タイマーと残り10秒警告を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- サーバー期限と表示誤差が許容内になる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：サーバー期限と表示誤差が許容内になる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-04：時間切れ時の自動パス・自動提出表示を作る

- 領域：EX
- 依存TODO：M5-SB-05
- 目的：時間切れ時の自動パス・自動提出表示を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「時間切れ時の自動パス・自動提出表示を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 実行結果と理由を全員が確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：実行結果と理由を全員が確認できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-05：切断猶予、棄権、残り1人勝利の表示を作る

- 領域：EX
- 依存TODO：M5-SB-06
- 目的：切断猶予、棄権、残り1人勝利の表示を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「切断猶予、棄権、残り1人勝利の表示を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 状態変化を誤解なく表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：状態変化を誤解なく表示できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-06：公式結果・公式勝利数画面を作る

- 領域：EX
- 依存TODO：M5-SB-07、08
- 目的：公式結果・公式勝利数画面を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式結果・公式勝利数画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 1局の結果と更新後勝利数を確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：1局の結果と更新後勝利数を確認できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-07：公式順位画面を作る

- 領域：EX
- 依存TODO：M5-SB-09
- 目的：公式順位画面を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式順位画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 順位、勝利数、参考勝率を表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：順位、勝利数、参考勝率を表示できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-EX-08：公式対戦プロフィール識別を作る

- 領域：EX
- 依存TODO：M5-SB-07
- 目的：公式対戦プロフィール識別を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式対戦プロフィール識別を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- プレイヤーIDと表示名を正しく表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：プレイヤーIDと表示名を正しく表示できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-GR-01：公式対戦、マッチング、順位の視覚体系を作る

- 領域：GR
- 依存TODO：M4-GR-01
- 目的：公式対戦、マッチング、順位の視覚体系を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式対戦、マッチング、順位の視覚体系を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- フレンド対戦と区別できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：フレンド対戦と区別できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-GR-02：30秒タイマー・10秒警告・棄権の演出素材を作る

- 領域：GR
- 依存TODO：M5-EX-03～05
- 目的：30秒タイマー・10秒警告・棄権の演出素材を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「30秒タイマー・10秒警告・棄権の演出素材を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 警告と棄権を色以外でも認識できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：警告と棄権を色以外でも認識できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-GR-03：公式勝利結果と順位表示素材を作る

- 領域：GR
- 依存TODO：M5-EX-06、07
- 目的：公式勝利結果と順位表示素材を作ることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公式勝利結果と順位表示素材を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 小画面でも勝者と更新値を確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：小画面でも勝者と更新値を確認できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-QA-01：マッチング同時参加・取消競合をテストする

- 領域：QA
- 依存TODO：M5-SB-01～03
- 目的：マッチング同時参加・取消競合をテストすることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「マッチング同時参加・取消競合をテストする」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 重複参加・5人目混入が0件。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：重複参加・5人目混入が0件。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-QA-02：タイマー境界29.9秒・30秒・再送をテストする

- 領域：QA
- 依存TODO：M5-SB-04、05
- 目的：タイマー境界29.9秒・30秒・再送をテストすることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「タイマー境界29.9秒・30秒・再送をテストする」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 1回だけ正しい自動処理が行われる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：1回だけ正しい自動処理が行われる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-QA-03：切断59秒・61秒・3回時間切れをテストする

- 領域：QA
- 依存TODO：M5-SB-06
- 目的：切断59秒・61秒・3回時間切れをテストすることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「切断59秒・61秒・3回時間切れをテストする」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 復帰と棄権が要件どおり分かれる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：復帰と棄権が要件どおり分かれる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-QA-04：結果・勝利数の同時更新と再送をテストする

- 領域：QA
- 依存TODO：M5-SB-08
- 目的：結果・勝利数の同時更新と再送をテストすることで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「結果・勝利数の同時更新と再送をテストする」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 二重加算・結果欠落が0件。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：二重加算・結果欠落が0件。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。

---

## M5-QA-05：招待テスターで100局実施する

- 領域：QA
- 依存TODO：M5-QA-01～04
- 目的：招待テスターで100局実施することで、将来版：人間4人の公式対戦の到達状態である「v1.0対象外の公式対戦・ランキングを将来実装できる詳細バックログを保持する」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「招待テスターで100局実施する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 重大同期・公式集計不具合が0件。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：重大同期・公式集計不具合が0件。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- v1.0のリリースゲートには含めない。実装開始時に当時のストア・認証・ランキング要件を再確認する。
- HTTPS招待リンク、Android App Links、未導入時の案内ページ→Google Playを前提とする。
