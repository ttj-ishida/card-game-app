# M1-EX-05 進捗

- TODO: M1-EX-05
- 状態: 完了
- 日付: 2026-08-29

## 概要

属性ロックと追加封印を `evaluateNumberPlay()` の判定条件へ追加した。ロック属性以外の候補、追加封印中の追加を不正にする。

## 成果物

| 種別 | パス                                       |
| ---- | ------------------------------------------ |
| 実装 | `packages/game-core/src/index.ts`          |
| test | `packages/game-core/src/playRules.test.ts` |

## 確認

| コマンド                      | 結果 |
| ----------------------------- | ---- |
| `npm run game-core:test`      | PASS |
| `npm run game-core:typecheck` | PASS |

## メモ

- 3枚以上かつ全カード同属性の結果では `createsSuitLock` と `lockedSuitCode` を返す。
- 場流しによる解除はM1-EX-08で扱う。
