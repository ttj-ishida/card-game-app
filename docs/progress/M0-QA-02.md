# M0-QA-02 Progress

## Summary

Reproduced the M0 setup in a separate local clone. Dependency installation, app checks, Android export, isolated Supabase migrations/seed, and DB tests all passed. Added .gitattributes to make Windows checkouts keep LF line endings so Prettier verification is reproducible.

## Outputs

| Output | Path |
|---|---|
| Setup reproduction script | scripts/qa-m0-setup-repro.ps1 |
| Line-ending normalization | .gitattributes |
| QA report | docs/qa/M0-QA-02-setup-reproduction-report.md |

## Verification

| Check | Result |
|---|---|
| Fresh local clone | PASS |
| npm ci | PASS |
| npm --prefix apps/mobile ci | PASS with moderate npm audit warnings |
| npm run assets:check | PASS |
| npm run ui:test | PASS, 4 tests |
| npm run ui:typecheck | PASS |
| npm run mobile:test | PASS, 12 tests |
| npm run mobile:typecheck | PASS |
| npm run mobile:lint | PASS |
| npm run mobile:format:check | PASS |
| npx expo export --platform android --output-dir dist | PASS |
| isolated npx supabase start | PASS |
| master_schema DB test | PASS, 20 tests |
| master_seed DB test | PASS, 19 tests |
| master_access DB test | PASS, 24 tests |
| isolated npx supabase stop | PASS |
| High severity known defects | 0 |

## Notes

- The first reproduction clone exposed a Windows line-ending issue, fixed by .gitattributes.
- Supabase DB reset was verified on an isolated temp project_id and alternate ports to avoid touching the active local development stack.
- The isolated Supabase stack was stopped after the run.

## Next

M0 is complete. Continue with M1 planning and implementation.
