# M1-SB-01 進捗

- TODO: M1-SB-01
- 状態: 完了
- 日付: 2026-08-29

## 概要

有効なルール版を取得するDB関数 `public.get_active_ruleset()` を追加した。表示名そのものではなく、`internal_code`、`ruleset_version`、resource keyを返す。

## 成果物

| 種別      | パス                                                                  |
| --------- | --------------------------------------------------------------------- |
| migration | `supabase/migrations/20260829090000_create_active_ruleset_lookup.sql` |
| DB test   | `supabase/tests/ruleset_version.sql`                                  |

## 確認

| コマンド                                                          | 結果          |
| ----------------------------------------------------------------- | ------------- |
| `npx supabase migration up --local`                               | PASS          |
| `npx supabase test db --local supabase/tests/ruleset_version.sql` | PASS、7 tests |

## メモ

- `anon` と `authenticated` に `EXECUTE` を許可した。
- 本番向けのルール版切替履歴や旧版互換管理は、M3以降の結果保存・運用設計で拡張する。
