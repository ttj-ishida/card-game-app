# M4-SB-11: ルーム・対局のRLSテスト

## 状態

完了。

## 実装内容

- RLS横断テスト `online_rls_cross_room.sql` を追加した。
- 2つの別ルームを作成し、片方の参加者が他ルーム、他ラウンド、他人の手札・スキルを読めないことを確認した。
- 同じルーム内では公開情報として参加者・席・公開状態・公開イベントを読める一方、非公開情報は本人分だけに制限されることを確認した。
- `get_friend_round_snapshot` も他ラウンドには使えないことを確認した。

## 確認結果

- `npx supabase test db supabase/tests/online_rls_cross_room.sql`: PASS（1 file / 10 tests）
- `npx supabase test db`: PASS（26 files / 450 tests）

## 後続

- `M4-SB-12`: フレンド対戦の退出・棄権・任意CPU引継ぎを実装する。
- その後、M4-EX系でルーム作成、参加、待機、オンライン対局表示へ接続する。
