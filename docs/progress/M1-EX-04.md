# M1-EX-04 進捗

- TODO: M1-EX-04
- 状態: 完了
- 日付: 2026-08-29

## 概要

追加と更新の合法性判定 `evaluateNumberPlay()` を追加した。場が空のリード、単体更新、同数追加、連番拡張、同種同枚数更新を判定する。

## 成果物

| 種別 | パス                                       |
| ---- | ------------------------------------------ |
| 実装 | `packages/game-core/src/index.ts`          |
| test | `packages/game-core/src/playRules.test.ts` |

## 確認

| コマンド                      | 結果 |
| ----------------------------- | ---- |
| `npm run game-core:test`      | PASS |
| `npm run game-core:typecheck` | PASS |

## メモ

- 不正時は `reason` を返し、入力状態は変更しない。
- 実際に手札を消費して局状態へ反映する処理はM1-EX-09で扱う。
