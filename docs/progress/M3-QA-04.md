# M3-QA-04: CPUアルファの配布用ビルドを作る

状態: 配布ビルド設定済み（EASログイン後にAPK生成）
日付: 2026-09-03

## 概要

配布用ビルドへ進む前の自動プリフライトを追加し、2026-09-03に自然革命修正後の最新HEADで再実行した。ルール、モバイル純関数・ストア、型、lint、format、Android export は通過している。M3-QA-02 はユーザー実機確認済みになったため、次はEAS内部配布APKを生成する。現PCではJavaがPATHになくローカルGradleビルドは不可、EAS CLI は未ログインのためAPK生成はログイン後に実行する。

## 成果物

| 種別                   | パス                                               |
| ---------------------- | -------------------------------------------------- |
| プリフライトレポート   | `docs/qa/M3-QA-04-distribution-build-preflight.md` |
| 一括プリフライトscript | `package.json` の `qa:m3:preflight`                |
| EAS Build設定 | `apps/mobile/eas.json`、`mobile:build:android:preview` |

## 確認

| コマンド                  | 結果                                                                                   |
| ------------------------- | -------------------------------------------------------------------------------------- |
| `npm run qa:m3:preflight` | PASS（`game-core:test` 210件、`mobile:test` 251件、typecheck/lint/format/export 通過） |

## 次の作業

`eas login` 後に `npm run mobile:build:android:preview` を実行して内部配布APKを生成し、テスター端末でインストール・1局完走・履歴/戦績保存を確認する。
