# M4-QA-05 退出・60秒再接続・CPU引継ぎを確認する

## Status

一部Done。棄権退出はハーネスで検証済み。60秒再接続はストアのユニットテストで検証済み
（M4-EX-07）。**CPU引き継ぎは検証不能 — サーバー未実装（下記）**。

## Summary

### 棄権退出（forfeit）— Done

M4-QA-01 ハーネスに `FORFEIT_AT` / `FORFEIT_CLIENT` を追加。指定手番でそのクライアントが
`leave_friend_round(roundId, requested_cpu_takeover=false)` を呼ぶ。ハーネスは以降その席を
飛ばして続行し、「残りプレイヤーの中から勝者が決まる」「棄権者は勝者にならない」を assert。

検証（ローカル）:
- 4人・P2 が turn 8 で棄権 → P1 勝利（33手、`1 forfeit`）。
- 2人・P2 が turn 4 で棄権（残り1人）→ `leave_friend_round` が即 `winner_player_id` を返し
  P1 勝利。
- 5人・障害注入併用・P3 が turn 10 で棄権 → P5 勝利（42手）。

### 60秒自動再接続 — Done（別作業 M4-EX-07）

`onlineRoundStore.test.ts` に「60秒経過で `connection:'offline'` 化しポーリング停止」
「`reconnect()` で全件再取得し復帰」のユニットテスト。

### CPU引き継ぎ — 未検証・サーバー未実装

`leave_friend_round(roundId, requested_cpu_takeover=true)` は席を `status='CPU_TAKEOVER'` /
`seat_kind='CPU'` / `auth_user_id=null` にするが、**その席の手番を代わりに打つサーバー処理が
存在しない**（CPU-turn driver の edge function / cron が無い）。`submit-play` の認証は
`auth_user_id` 一致必須なので、誰も CPU 席を進められず対局が停止する。

M4 完了には別途「CPU席の手番を driver で打つ」実装が必要（`packages/game-core` の
`standardPolicy` を server で呼ぶ形になる想定）。クライアント表示（「CPU操作中」タグ）は
M4-EX-09 で対応済み。

## Follow-up

- CPU引き継ぎ driver（SB追加）。着手するまで、退出時は棄権（`requested_cpu_takeover=false`）
  のみ実質機能する。UI の退出ボタンは現状 `leaveRound(true)` を呼ぶので、driver 未実装の
  うちは `false` に倒すか、driver を先に実装するか判断が必要。
