# M0-SB-01 実施記録

- Status: DONE
- Completed: 2026-08-28
- Owner: Project owner
- Related decision: ADR-0002

## 実施内容

- Supabase CLIをnpm dev dependencyとして導入した。
- Supabase CLIのログイン状態を確認した。
- Supabase組織一覧を確認し、`card-game-app`組織を確認した。
- Supabaseプロジェクト一覧を確認し、`card-game-app`プロジェクトが`ACTIVE_HEALTHY`であることを確認した。
- 既存の`card-game-app`プロジェクトをADR-0002の`development`用Supabaseプロジェクトとして採用した。
- 秘密情報やローカル依存物をGitへ含めないため、`.gitignore`を追加した。

## 採用したSupabase開発プロジェクト

| 項目 | 値 |
|---|---|
| 環境 | `development` |
| Supabase organization | `card-game-app` |
| Organization ID | `mochidqhqgiabzmvcgqo` |
| Project name | `card-game-app` |
| Project ref | `evzmtxwdsoebekxlqxeo` |
| Region | `ap-northeast-2` |
| Status | `ACTIVE_HEALTHY` |

## 受入結果

| 確認項目 | 結果 | 証跡 |
|---|---|---|
| 開発用接続情報で疎通できる | PASS | `npx supabase projects list`で対象プロジェクトを確認 |
| ADR-0002の`development`環境と対応する | PASS | 本記録「採用したSupabase開発プロジェクト」 |
| Supabase CLIを再現可能に実行できる | PASS | `package.json` dev dependency |
| 秘密情報を追加していない | PASS | `.gitignore`で`.env*`、`*password*.txt`、`*secret*.txt`を除外 |
| 本番環境を作成していない | PASS | `production`はM8まで対象外 |

## 注意点

- Project URL、anon key、service role key、DB passwordの実値は本リポジトリへ記録しない。
- `spabase_password.txt`のようなローカル秘密ファイルはGitへ追加しない。
- `node_modules/`はGitへ追加しない。
- Supabase local stackの初期化、`supabase/config.toml`、マイグレーション実行基盤は`M0-SB-02`で扱う。

## 次のTODO

`M0-SB-02`で、Supabaseローカル構成とマイグレーション実行基盤を用意し、空DBから同じ構造を再現できる状態にする。
