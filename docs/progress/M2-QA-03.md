# M2-QA-03 進捗

- TODO: M2-QA-03
- 状態: 自動確認完了・実機確認待ち
- 日付: 2026-09-01

## 概要

手札配置・レイアウト確認。`boardViewModel.test.ts` の 2 人 18 枚 / 6 人 6 枚構造テストで自動確認完了。実機目視チェックリスト（スマホ・タブレット横画面、手札重なり・欠け・タップ領域、相手席表示、文字拡大、TalkBack）はユーザーが手動確認。

## 成果物

| 種別 | パス |
| --- | --- |
| QA レポート | `docs/qa/M2-QA-03-hand-layout-report.md` |
| テスト対象 | `apps/mobile/src/features/cpu-game/boardViewModel.test.ts` |

## 確認（自動）

| コマンド | 結果 |
| --- | --- |
| `npm run mobile:test` | PASS（172 件）— M2-QA-03 テスト含む |
| `npm run mobile:typecheck` | PASS |
| `npm run mobile:lint` | PASS |
| `npm run mobile:format:check` | PASS |
| `npm run game-core:test` | PASS（184 件、回帰なし） |
| `npm run game-core:typecheck` | PASS |
| `npm run ui:test` | PASS（4 件） |
| `npm run ui:typecheck` | PASS |
| `npx expo export --platform android` | PASS |
| `git diff --check` | 問題なし |

## メモ

- **自動テスト** — 2 人配布で `vm.hand.length === 18` / 6 人配布で `vm.hand.length === 6` を検証。全カード要素の型チェック（`rank`, `suitCode`, `isJoker`）。手札整列順（`(rank, suitCode)` 昇順）。場表示の生成。昼夜による強弱順反転。対手パネル席順。
- **実機確認** — スマホ（6 インチ）・タブレット（10 インチ）相当での横画面レイアウト。手札 18 枚の重なり・欠け・タップ領域。相手 6 席の横スクロール。文字拡大での操作ボタン欠けなし。TalkBack 読み上げ順。CPU「思考中…」表示。結果画面遷移。戻る操作。
- **プレースホルダ** — M2 の画面は機能プレースホルダ版。デザイン版は `boardViewModel` の出力形を読んで新 `.tsx` を作るだけ。

## テーブル

### 自動テスト（boardViewModel.test.ts）

| テスト | 検証内容 | 結果 |
| --- | --- | --- |
| 2 人配布 = 18 枚 | hand.length | PASS |
| 6 人配布 = 6 枚 | hand.length | PASS |
| 全 HandCardView 形状 | { cardId, rank, suitCode, isJoker, selected, selectable } | PASS |
| 全カード識別要素 | { rank, suitCode, isJoker } | PASS |
| 手札整列順 | (rank, suitCode) 昇順 | PASS |
| 場表示 | null → FieldView | PASS |
| 強弱順 昼 | [1..9] | PASS |
| 強弱順 夜 | [9..1] | PASS |
| 対手パネル | 人間席除外・席順・status・isActive | PASS |

### 実機確認チェックリスト（ユーザー手動）

| # | 項目 | 期待 | 結果 |
| --- | --- | --- | --- |
| D-1 | スマホ横画面 18 枚 | 手札が重なり・欠け・タップ領域不足なく | 未確認 |
| D-2 | スマホ横画面 6 枚 | カードが適切間隔で配置 | 未確認 |
| D-3 | タブレット横画面 | 広く読みやすく、崩れなし | 未確認 |
| D-4 | 相手席 6 席 | 横スクロールで全て見える | 未確認 |
| D-5 | 場のカードとロック | 数字・属性色・ラベルが読める | 未確認 |
| D-6 | 文字拡大時ボタン | 「出す」「パス」「選択解除」が欠けず | 未確認 |
| D-7 | TalkBack 読み上げ | フォーカス順「上帯→相手→場→手札→操作」、カード「数字・属性」 | 未確認 |
| D-8 | CPU「思考中…」 | phase=CPU_PENDING で表示、操作無効 | 未確認 |
| D-9 | 結果画面遷移 | ROUND_OVER で `/cpu-game/result`、勝敗表示 | 未確認 |
| D-10 | 戻る操作 | 確認ダイアログ → ホームへ | 未確認 |

## 将来への申し送り

- **実機確認作業** — 上記チェックリスト（D-1～D-10）を実機（スマホ・タブレット）で埋める。各行末「未確認」を「確認済み」に更新。
- **デザイン版への差し替え** — 新 `play.tsx` は同じ `boardViewModel` を読んで描画。`features/cpu-game/boardViewModel.ts` は無変更。
- **複数パック対応** — M3-EX-08（ソロ設定）で `CardFace` に `packId` を渡す実装。M2 は常に `packId='DEFAULT'`。
