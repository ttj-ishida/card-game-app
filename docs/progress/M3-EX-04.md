# M3-EX-04: 対局履歴画面

## 状態

完了。CPU戦の保存済み結果と公開イベントを読み取り、`/cpu-game/history` で確認できる。

## 実装

- `apps/mobile/src/features/cpu-game/historyModel.ts`
  - `practice_round_results` を匿名プレイヤーID + `CPU_PRACTICE` で最新順に取得。
  - `round_events` を `round_result_id` で追加取得。
  - 公開イベントだけを画面用 ViewModel に変換。
- `apps/mobile/src/state/cpuGameHistoryStore.ts`
  - `getAnonPlayerId` と Supabase 読み取り依存を注入して履歴をロード。
- `apps/mobile/src/app/cpu-game/history.tsx`
  - 読み込み中、空、失敗、一覧、イベント欠落を表示。
- `apps/mobile/src/app/_layout.tsx` / `apps/mobile/src/app/cpu-game/setup.tsx`
  - ルート定義と CPU戦セットアップ画面からの導線を追加。

## 確認

- `npx tsx --test src/features/cpu-game/historyModel.test.ts src/state/cpuGameHistoryStore.test.ts`
- `npm run mobile:test`
- `npm run mobile:typecheck`
- `npm run mobile:lint`
- `npm run mobile:format:check`
- `npm run mobile:export:android`

## 実機相当確認

- 2026-09-02: Android 起動後、CPU戦を最後まで完了し、結果画面で保存成功を確認。
- 2026-09-02: 対局履歴画面で保存済み対局が表示されることをユーザー確認済み。

## 残り

- M3-QA-02 / M3-QA-04 では、全スキル境界や配布用ビルド上での再確認を行う。
