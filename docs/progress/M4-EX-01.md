# M4-EX-01 フレンド対戦ルーム作成画面

## Status

Done.

## Summary

- Home から `フレンド対戦` へ遷移できる入口を追加した。
- 招待コード、人数、制限時間、退出時 CPU 引継ぎの設定でルーム作成できる画面を追加した。
- Supabase Auth の匿名セッションを取得・保存し、`create_friend_room` / `room_players` へ接続する mobile client と store を追加した。
- Supabase 設定や通信失敗時は画面上の日本語エラーに落とす。

## Verification

- `apps/mobile/src/features/online-room/onlineRoomClient.test.ts` を追加。
- `apps/mobile/src/state/onlineRoomStore.test.ts` を追加。
- 実行確認はこの作業単位のコミット前に `mobile:test` / `mobile:typecheck` / `mobile:lint` / `mobile:format:check` で行う。

## Follow-up

- M4-EX-02: HTTPS 招待リンク対応は未完了。現時点では招待コードの手入力参加まで。
- M4-EX-03: 待機室 UI は作成済みだが、リアルタイム購読とオンライン対局画面への接続は後続で仕上げる。