# M0-SB-02 実施記録

- Status: DONE
- Completed: 2026-08-28
- Owner: Project owner
- Related decision: ADR-0002

## 実施内容

- `npx supabase init`でSupabaseローカル構成を初期化した。
- `supabase/config.toml`を作成し、ローカルDB、API、Studio、Auth、Storage、Realtimeの標準設定を導入した。
- `development`用Supabaseプロジェクト`evzmtxwdsoebekxlqxeo`へ`supabase link`した。
- マイグレーション、Edge Functions、DBテストの配置先として`supabase/migrations`、`supabase/functions`、`supabase/tests`を用意した。
- `supabase/seed.sql`を作成し、後続TODOで開発用シードを追加できる状態にした。
- npm scriptsへSupabaseローカル操作の入口を追加した。

## 成果物

| 種別 | パスまたはコマンド | 用途 |
|---|---|---|
| 設定 | `supabase/config.toml` | Supabaseローカル構成 |
| マイグレーション | `supabase/migrations/` | DB変更の正本 |
| Functions | `supabase/functions/` | Edge Functions配置先 |
| テスト | `supabase/tests/` | DB、RLS、サーバー処理テスト配置先 |
| シード | `supabase/seed.sql` | 開発・検証用の非秘密シード |
| scripts | `npm run db:start` | ローカルSupabase起動 |
| scripts | `npm run db:stop` | ローカルSupabase停止 |
| scripts | `npm run db:status` | ローカルSupabase状態確認 |
| scripts | `npm run db:reset` | マイグレーションとシードの再適用 |
| scripts | `npm run db:link` | developmentプロジェクトへの再リンク |

## 受入結果

| 確認項目 | 結果 | 証跡 |
|---|---|---|
| 空DBから同じ構造を再現できる基盤がある | PASS | `supabase/config.toml`、`supabase/migrations/`、`supabase/seed.sql` |
| developmentプロジェクトへリンクできる | PASS | `npx supabase link --project-ref evzmtxwdsoebekxlqxeo` |
| CLIを再現可能に実行できる | PASS | `npx supabase --version` = `2.116.0` |
| ローカル状態確認コマンドがある | PASS | `npm run db:status` |
| 秘密情報を追加していない | PASS | Project refのみ記録し、DB passwordやAPI keyは未記録 |
| ローカルSupabaseを起動できる | BLOCKED | DockerまたはPodmanがPATHにないため未起動 |

## 未実施・理由

- `npm run db:start`と`npm run db:reset`は、Docker DesktopまたはPodmanがPATHにないため未実行。
- 現時点ではマイグレーションが未作成のため、DB構造の実体は後続の`M0-SB-03`で追加する。

## 次のTODO

`M0-SB-03`で、`rulesets`、`terms`、`number_cards`、`skill_cards`のマスタをマイグレーションとして作成する。
