# M4-SB-09: 公開状態・本人手札・イベントのRealtime配信

## 状態

完了。

## 実装内容

- `online_round_public_state`、`online_round_events`、`round_hands`、`round_skills`をSupabase Realtimeの`supabase_realtime` publicationへ登録した。
- 更新通知で購読側が最新行を復元できるよう、対象テーブルを`REPLICA IDENTITY FULL`にした。
- 受信者別の情報制御は既存RLSを継続利用する。公開状態・公開イベントは対局参加者だけ、手札・スキルは所有者本人だけがSELECTできる。
- DBテスト `online_realtime_publication.sql` を追加し、publication登録とreplica identity設定を検証した。

## 確認結果

- `npx supabase db reset`: PASS
- `npx supabase test db supabase/tests/online_realtime_publication.sql`: PASS（1 file / 8 tests）
- `npx supabase test db`: PASS（24 files / 429 tests）

## 後続

- `M4-SB-10`: 最新スナップショット取得と再接続復旧を作る。
- `M4-EX-06`: Realtimeイベントの順序・欠落検知をクライアント側へ実装する。
