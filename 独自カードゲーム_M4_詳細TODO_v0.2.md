# M4 詳細TODO：2〜6人フレンド対戦

- 文書ID：GAME-TODO-DETAIL-M4
- 版数：0.2
- 基準文書：`独自カードゲーム_横断マイルストーン_TODO_v0.1.md`、`独自カードゲーム_要件定義書_v0.1.md`
- TODO件数：31件

## このマイルストーンの到達状態

招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる。

## 確定前提

- 参加方式はHTTPS招待リンク。未導入時は案内ページからGoogle Playへ誘導する。
- 案内ページはSupabase中心で構築し、Android App Linksを使用する。
- 作成者退出時は最も早く参加した残存プレイヤーへ権限移譲する。
- CPU引継ぎ時、退出者は敗北、最終勝者は勝利として記録する。

## 共通実施ルール

- 依存TODOが未完了の場合は着手しない。並行可能な場合は、共有インターフェースだけ先に固定する。
- 表示名・日本語文言を内部IDや対局状態へ保存しない。安定IDと言語リソースを使用する。
- 非公開手札、未使用スキル、認証情報、個人情報をクライアントログや分析イベントへ出さない。
- Supabase変更はマイグレーション、Expo変更は自動テスト、グラフィック変更はアセット対応表を必須とする。
- 曖昧さや確定仕様との矛盾を発見した場合は作業を止め、要件の決定者へ確認する。

## TODO一覧

| TODO ID | 領域 | 概要 | 依存 |
|---|---|---|---|
| M4-ARC-01 | 横断 | サーバー権威方式の技術スパイクを行う | M3-QA-04 |
| M4-ARC-02 | 横断 | ルールエンジンをExpoとサーバーから共有できるパッケージへ分離する | M4-ARC-01 |
| M4-SB-01 | SB | `rooms`、`room_players`、`rounds`、`round_players`を作る | M4-ARC-01 |
| M4-SB-02 | SB | `round_hands`、`round_skills`を所有者別に作る | M4-SB-01 |
| M4-SB-03 | SB | `round_public_state`と`round_events`を作る | M4-SB-01 |
| M4-SB-04 | SB | ルーム作成・参加・退出のサーバー処理を作る | M4-SB-01 |
| M4-SB-05 | SB | シャッフル・配布・先攻決定のサーバー処理を作る | M4-SB-01～03 |
| M4-SB-06 | SB | プレイ要求用Edge Functionを作る | M4-ARC-02、M4-SB-03 |
| M4-SB-07 | SB | 期待状態版による条件付きコミットを1つのDBトランザクションで作る | M4-SB-06 |
| M4-SB-08 | SB | リクエストIDによる冪等性を作る | M4-SB-06 |
| M4-SB-09 | SB | 公開状態・本人手札・イベントのRealtime配信を作る | M4-SB-02、M4-SB-03 |
| M4-SB-10 | SB | 最新スナップショット取得と再接続処理を作る | M4-SB-09 |
| M4-SB-11 | SB | ルーム・対局のRLSテストを作る | M4-SB-01～10 |
| M4-SB-12 | SB | フレンド対戦の退出・棄権・任意CPU引継ぎを作る | M4-SB-01、M2-EX-03 |
| M4-EX-01 | EX | フレンドルーム作成画面を作る | M4-SB-04 |
| M4-EX-02 | EX | HTTPS招待リンクによる参加画面を作る | M4-SB-04 |
| M4-EX-03 | EX | 参加者一覧と開始待機画面を作る | M4-SB-04 |
| M4-EX-04 | EX | オンライン対局状態をローカル表示状態へ変換する | M4-SB-09、M4-ARC-02 |
| M4-EX-05 | EX | プレイ要求、受付中、成立、不成立のUIを作る | M4-SB-06～08 |
| M4-EX-06 | EX | Realtimeイベントの順序・欠落検知を作る | M4-SB-09 |
| M4-EX-07 | EX | 切断表示と60秒以内の自動再接続を作る | M4-SB-10 |
| M4-EX-08 | EX | オンライン結果と再戦意思確認を作る | M4-SB-03 |
| M4-EX-09 | EX | フレンド対戦の退出・CPU引継ぎ表示を作る | M4-SB-12 |
| M4-GR-01 | GR | ルーム、席、準備状態のUI素材を作る | M3-GR-01～04 |
| M4-GR-02 | GR | 通信中、再接続中、同期失敗の表示素材を作る | M4-EX-07 |
| M4-GR-03 | GR | 相手の裏向き手札とスキル保有表示を仕上げる | M0-GR-04 |
| M4-QA-01 | QA | 2～6クライアントの自動操作ハーネスを作る | M4-SB-06～09 |
| M4-QA-02 | QA | 遅延、重複、順序入替、切断を注入する | M4-QA-01 |
| M4-QA-03 | QA | 50局のオンライン連続対戦を行う | M4-QA-01、02 |
| M4-QA-04 | QA | 招待制αビルドを配布する | M4-QA-03 |
| M4-QA-05 | QA | 退出・60秒再接続・CPU引継ぎを確認する | M4-SB-10、12 |

---

## M4-ARC-01：サーバー権威方式の技術スパイクを行う

- 領域：横断
- 依存TODO：M3-QA-04
- 目的：サーバー権威方式の技術スパイクを行うことで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「サーバー権威方式の技術スパイクを行う」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M4-ARC-01` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- Edge Function＋共有TSルール＋条件付きDB更新の採否が決まる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：Edge Function＋共有TSルール＋条件付きDB更新の採否が決まる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-ARC-02：ルールエンジンをExpoとサーバーから共有できるパッケージへ分離する

- 領域：横断
- 依存TODO：M4-ARC-01
- 目的：ルールエンジンをExpoとサーバーから共有できるパッケージへ分離することで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ルールエンジンをExpoとサーバーから共有できるパッケージへ分離する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 同じテストを両実行環境で通せる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：同じテストを両実行環境で通せる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-01：`rooms`、`room_players`、`rounds`、`round_players`を作る

- 領域：SB
- 依存TODO：M4-ARC-01
- 目的：`rooms`、`room_players`、`rounds`、`round_players`を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`rooms`、`room_players`、`rounds`、`round_players`を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- ルームと1局の参加席を保存できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：ルームと1局の参加席を保存できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-02：`round_hands`、`round_skills`を所有者別に作る

- 領域：SB
- 依存TODO：M4-SB-01
- 目的：`round_hands`、`round_skills`を所有者別に作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`round_hands`、`round_skills`を所有者別に作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 本人以外が手札を読めない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：本人以外が手札を読めない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-03：`round_public_state`と`round_events`を作る

- 領域：SB
- 依存TODO：M4-SB-01
- 目的：`round_public_state`と`round_events`を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`round_public_state`と`round_events`を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 場・昼夜・手番・公開履歴を取得できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：場・昼夜・手番・公開履歴を取得できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-04：ルーム作成・参加・退出のサーバー処理を作る

- 領域：SB
- 依存TODO：M4-SB-01
- 目的：ルーム作成・参加・退出のサーバー処理を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ルーム作成・参加・退出のサーバー処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 人数上限と重複参加を制御できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：人数上限と重複参加を制御できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-05：シャッフル・配布・先攻決定のサーバー処理を作る

- 領域：SB
- 依存TODO：M4-SB-01～03
- 目的：シャッフル・配布・先攻決定のサーバー処理を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「シャッフル・配布・先攻決定のサーバー処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- クライアントがデッキ順を決められない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：クライアントがデッキ順を決められない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-06：プレイ要求用Edge Functionを作る

- 領域：SB
- 依存TODO：M4-ARC-02、M4-SB-03
- 目的：プレイ要求用Edge Functionを作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「プレイ要求用Edge Functionを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 認証、手番、状態版、合法性を検証できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：認証、手番、状態版、合法性を検証できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-07：期待状態版による条件付きコミットを1つのDBトランザクションで作る

- 領域：SB
- 依存TODO：M4-SB-06
- 目的：期待状態版による条件付きコミットを1つのDBトランザクションで作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「期待状態版による条件付きコミットを1つのDBトランザクションで作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 公開状態、手札、イベントの更新が全成功または全取消になる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：公開状態、手札、イベントの更新が全成功または全取消になる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-08：リクエストIDによる冪等性を作る

- 領域：SB
- 依存TODO：M4-SB-06
- 目的：リクエストIDによる冪等性を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「リクエストIDによる冪等性を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 同じ要求を再送しても1回だけ処理される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：同じ要求を再送しても1回だけ処理される。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-09：公開状態・本人手札・イベントのRealtime配信を作る

- 領域：SB
- 依存TODO：M4-SB-02、M4-SB-03
- 目的：公開状態・本人手札・イベントのRealtime配信を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「公開状態・本人手札・イベントのRealtime配信を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 受信者別の情報だけが配信される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：受信者別の情報だけが配信される。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-10：最新スナップショット取得と再接続処理を作る

- 領域：SB
- 依存TODO：M4-SB-09
- 目的：最新スナップショット取得と再接続処理を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「最新スナップショット取得と再接続処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 状態版不一致から復旧できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：状態版不一致から復旧できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-11：ルーム・対局のRLSテストを作る

- 領域：SB
- 依存TODO：M4-SB-01～10
- 目的：ルーム・対局のRLSテストを作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ルーム・対局のRLSテストを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 他ルーム・他人手札へアクセスできない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：他ルーム・他人手札へアクセスできない。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-SB-12：フレンド対戦の退出・棄権・任意CPU引継ぎを作る

- 領域：SB
- 依存TODO：M4-SB-01、M2-EX-03
- 目的：フレンド対戦の退出・棄権・任意CPU引継ぎを作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「フレンド対戦の退出・棄権・任意CPU引継ぎを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 退出者を敗北として記録し、設定に従って席を除外またはCPUへ引き継げる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：退出者を敗北として記録し、設定に従って席を除外またはCPUへ引き継げる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- v1.0のCPUは標準1種類。画面上の思考待ちは0.6〜1.2秒とする。

---

## M4-EX-01：フレンドルーム作成画面を作る

- 領域：EX
- 依存TODO：M4-SB-04
- 目的：フレンドルーム作成画面を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「フレンドルーム作成画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 人数・時間・CPU引継ぎを指定できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：人数・時間・CPU引継ぎを指定できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-02：HTTPS招待リンクによる参加画面を作る

- 領域：EX
- 依存TODO：M4-SB-04
- 目的：HTTPS招待リンクによる参加画面を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「HTTPS招待リンクによる参加画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 導入済み端末は入室し、未導入端末は案内ページからGoogle Playへ進める。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：導入済み端末は入室し、未導入端末は案内ページからGoogle Playへ進める。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- HTTPS招待リンク、Android App Links、未導入時の案内ページ→Google Playを前提とする。

---

## M4-EX-03：参加者一覧と開始待機画面を作る

- 領域：EX
- 依存TODO：M4-SB-04
- 目的：参加者一覧と開始待機画面を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「参加者一覧と開始待機画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 席・準備状態・設定が一致する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：席・準備状態・設定が一致する。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-04：オンライン対局状態をローカル表示状態へ変換する

- 領域：EX
- 依存TODO：M4-SB-09、M4-ARC-02
- 目的：オンライン対局状態をローカル表示状態へ変換することで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「オンライン対局状態をローカル表示状態へ変換する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- サーバー状態だけを正として描画できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：サーバー状態だけを正として描画できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-05：プレイ要求、受付中、成立、不成立のUIを作る

- 領域：EX
- 依存TODO：M4-SB-06～08
- 目的：プレイ要求、受付中、成立、不成立のUIを作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「プレイ要求、受付中、成立、不成立のUIを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 連打しても二重提出されない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：連打しても二重提出されない。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-06：Realtimeイベントの順序・欠落検知を作る

- 領域：EX
- 依存TODO：M4-SB-09
- 目的：Realtimeイベントの順序・欠落検知を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Realtimeイベントの順序・欠落検知を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 版飛びで自動再同期できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：版飛びで自動再同期できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-07：切断表示と60秒以内の自動再接続を作る

- 領域：EX
- 依存TODO：M4-SB-10
- 目的：切断表示と60秒以内の自動再接続を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「切断表示と60秒以内の自動再接続を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 復帰後に正しい手札と場が表示される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：復帰後に正しい手札と場が表示される。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-08：オンライン結果と再戦意思確認を作る

- 領域：EX
- 依存TODO：M4-SB-03
- 目的：オンライン結果と再戦意思確認を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「オンライン結果と再戦意思確認を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 全員が次局または退出へ進める。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：全員が次局または退出へ進める。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-EX-09：フレンド対戦の退出・CPU引継ぎ表示を作る

- 領域：EX
- 依存TODO：M4-SB-12
- 目的：フレンド対戦の退出・CPU引継ぎ表示を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「フレンド対戦の退出・CPU引継ぎ表示を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 引継ぎ席と以後の操作主体を確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：引継ぎ席と以後の操作主体を確認できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- v1.0のCPUは標準1種類。画面上の思考待ちは0.6〜1.2秒とする。

---

## M4-GR-01：ルーム、席、準備状態のUI素材を作る

- 領域：GR
- 依存TODO：M3-GR-01～04
- 目的：ルーム、席、準備状態のUI素材を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ルーム、席、準備状態のUI素材を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 2～6人で見切れず状態が分かる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：2～6人で見切れず状態が分かる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-GR-02：通信中、再接続中、同期失敗の表示素材を作る

- 領域：GR
- 依存TODO：M4-EX-07
- 目的：通信中、再接続中、同期失敗の表示素材を作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「通信中、再接続中、同期失敗の表示素材を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 色だけに頼らず状態を示せる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：色だけに頼らず状態を示せる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-GR-03：相手の裏向き手札とスキル保有表示を仕上げる

- 領域：GR
- 依存TODO：M0-GR-04
- 目的：相手の裏向き手札とスキル保有表示を仕上げることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「相手の裏向き手札とスキル保有表示を仕上げる」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 残り枚数と保有有無を明確に表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：残り枚数と保有有無を明確に表示できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-QA-01：2～6クライアントの自動操作ハーネスを作る

- 領域：QA
- 依存TODO：M4-SB-06～09
- 目的：2～6クライアントの自動操作ハーネスを作ることで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「2～6クライアントの自動操作ハーネスを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 複数席を再現可能なテストがある。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：複数席を再現可能なテストがある。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-QA-02：遅延、重複、順序入替、切断を注入する

- 領域：QA
- 依存TODO：M4-QA-01
- 目的：遅延、重複、順序入替、切断を注入することで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「遅延、重複、順序入替、切断を注入する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 状態不一致を検知・復旧できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：状態不一致を検知・復旧できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-QA-03：50局のオンライン連続対戦を行う

- 領域：QA
- 依存TODO：M4-QA-01、02
- 目的：50局のオンライン連続対戦を行うことで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「50局のオンライン連続対戦を行う」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 二重消費、停止、情報漏洩が0件。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：二重消費、停止、情報漏洩が0件。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M4-QA-04：招待制αビルドを配布する

- 領域：QA
- 依存TODO：M4-QA-03
- 目的：招待制αビルドを配布することで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「招待制αビルドを配布する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 複数実端末で再現手順どおり完走できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：複数実端末で再現手順どおり完走できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- HTTPS招待リンク、Android App Links、未導入時の案内ページ→Google Playを前提とする。

---

## M4-QA-05：退出・60秒再接続・CPU引継ぎを確認する

- 領域：QA
- 依存TODO：M4-SB-10、12
- 目的：退出・60秒再接続・CPU引継ぎを確認することで、2〜6人フレンド対戦の到達状態である「招待リンクから参加し、複数端末で同期・再接続しながら1局完走できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「退出・60秒再接続・CPU引継ぎを確認する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 設定どおり復帰または引継ぎされる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：設定どおり復帰または引継ぎされる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- v1.0のCPUは標準1種類。画面上の思考待ちは0.6〜1.2秒とする。
