# M7 詳細TODO：広告・分析・運用

- 文書ID：GAME-TODO-DETAIL-M7
- 版数：0.2
- 基準文書：`独自カードゲーム_横断マイルストーン_TODO_v0.1.md`、`独自カードゲーム_要件定義書_v0.1.md`
- TODO件数：19件

## このマイルストーンの到達状態

対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる。

## 確定前提

- 毎局終了後に全画面広告を表示する。対局進行中は表示しない。
- 報酬広告完了で次回広告スキップ権を1回分だけ付与し、自動消費する。買い切り広告除去は実装しない。
- AdMob、Firebase Analytics、Crashlyticsを使用する。
- 退会時は本人の対局イベントを削除し、他参加者の勝敗集計値だけ維持する。

## 共通実施ルール

- 依存TODOが未完了の場合は着手しない。並行可能な場合は、共有インターフェースだけ先に固定する。
- 表示名・日本語文言を内部IDや対局状態へ保存しない。安定IDと言語リソースを使用する。
- 非公開手札、未使用スキル、認証情報、個人情報をクライアントログや分析イベントへ出さない。
- Supabase変更はマイグレーション、Expo変更は自動テスト、グラフィック変更はアセット対応表を必須とする。
- 曖昧さや確定仕様との矛盾を発見した場合は作業を止め、要件の決定者へ確認する。

## TODO一覧

| TODO ID | 領域 | 概要 | 依存 |
|---|---|---|---|
| M7-PM-01 | 横断 | 確定した広告位置・頻度・報酬・非表示課金なしを仕様化する | M4-QA-05 |
| M7-PM-02 | 横断 | 収集イベント、13歳以上、全世界、日本語のみの同意要件を確定する | M7-PM-01 |
| M7-SB-01 | SB | 分析イベントの命名とスキーマを作る | M7-PM-02 |
| M7-SB-02 | SB | 対局IDを使った障害・監査検索を整備する | M4-SB-03、M4-SB-06～08 |
| M7-SB-03 | SB | 退会・データ削除のサーバー処理を作る | M7-PM-02 |
| M7-SB-04 | SB | サービス停止・フレンド対戦受付停止フラグを作る | M4-SB-04 |
| M7-SB-05 | SB | エラー率、対局成立数、切断率の監視を作る | M7-SB-02 |
| M7-EX-01 | EX | 広告SDKと同意処理を組み込む | M7-PM-01、02 |
| M7-EX-02 | EX | 毎局の結果後に全画面広告を実装する | M7-EX-01 |
| M7-EX-03 | EX | 広告失敗・タイムアウト時の継続処理を作る | M7-EX-02 |
| M7-EX-04 | EX | 分析イベントとクラッシュレポートを組み込む | M7-SB-01 |
| M7-EX-05 | EX | 利用規約、プライバシー、同意画面を作る | M7-PM-02 |
| M7-EX-06 | EX | 退会・データ削除導線を作る | M7-SB-03 |
| M7-EX-07 | EX | メンテナンス・障害・更新必須画面を作る | M7-SB-04 |
| M7-GR-01 | GR | 同意、メンテナンス、通信障害の画面素材を作る | M7-EX-05～07 |
| M7-GR-02 | GR | アプリアイコン、起動画面、ストア画像の初稿を作る | M6-GR-01～08 |
| M7-QA-01 | QA | 広告成功・失敗・オフライン・同意拒否をテストする | M7-EX-01～03 |
| M7-QA-02 | QA | 分析イベントの欠落・重複・個人情報を確認する | M7-EX-04 |
| M7-QA-03 | QA | 退会・規約更新・メンテナンスをE2E確認する | M7-EX-05～07 |

---

## M7-PM-01：確定した広告位置・頻度・報酬・非表示課金なしを仕様化する

- 領域：横断
- 依存TODO：M4-QA-05
- 目的：確定した広告位置・頻度・報酬・非表示課金なしを仕様化することで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「確定した広告位置・頻度・報酬・非表示課金なしを仕様化する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M7-PM-01` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- 毎局後広告、報酬スキップ権1回、買い切りなしが要件化される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：毎局後広告、報酬スキップ権1回、買い切りなしが要件化される。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。

---

## M7-PM-02：収集イベント、13歳以上、全世界、日本語のみの同意要件を確定する

- 領域：横断
- 依存TODO：M7-PM-01
- 目的：収集イベント、13歳以上、全世界、日本語のみの同意要件を確定することで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「収集イベント、13歳以上、全世界、日本語のみの同意要件を確定する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 選択肢を最低2案挙げ、開発工数、運用費、セキュリティ、将来拡張、利用者体験で比較する。
3. 採用案、非採用理由、見直し条件をADRまたは仕様メモとして残し、影響する後続TODOの前提を更新する。
4. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- `M7-PM-02` 意思決定記録
- 更新済みの仕様・依存TODO一覧

### 完了条件

- 地域別のデータ収集・同意一覧が完成する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：地域別のデータ収集・同意一覧が完成する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-SB-01：分析イベントの命名とスキーマを作る

- 領域：SB
- 依存TODO：M7-PM-02
- 目的：分析イベントの命名とスキーマを作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「分析イベントの命名とスキーマを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 版管理されたイベント一覧がある。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：版管理されたイベント一覧がある。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-SB-02：対局IDを使った障害・監査検索を整備する

- 領域：SB
- 依存TODO：M4-SB-03、M4-SB-06～08
- 目的：対局IDを使った障害・監査検索を整備することで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「対局IDを使った障害・監査検索を整備する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 1局の操作を時系列で追跡できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：1局の操作を時系列で追跡できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-SB-03：退会・データ削除のサーバー処理を作る

- 領域：SB
- 依存TODO：M7-PM-02
- 目的：退会・データ削除のサーバー処理を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「退会・データ削除のサーバー処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 本人の対局イベントを削除し、他参加者の勝敗集計値だけ維持できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：本人の対局イベントを削除し、他参加者の勝敗集計値だけ維持できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 本人の対局イベントを削除し、他参加者には勝敗集計値のみ残す。削除前後の再計算と参照切れを確認する。

---

## M7-SB-04：サービス停止・フレンド対戦受付停止フラグを作る

- 領域：SB
- 依存TODO：M4-SB-04
- 目的：サービス停止・フレンド対戦受付停止フラグを作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「サービス停止・フレンド対戦受付停止フラグを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 新規フレンド対戦受付を安全に止められる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：新規フレンド対戦受付を安全に止められる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-SB-05：エラー率、対局成立数、切断率の監視を作る

- 領域：SB
- 依存TODO：M7-SB-02
- 目的：エラー率、対局成立数、切断率の監視を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「エラー率、対局成立数、切断率の監視を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 閾値超過を検知できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：閾値超過を検知できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-EX-01：広告SDKと同意処理を組み込む

- 領域：EX
- 依存TODO：M7-PM-01、02
- 目的：広告SDKと同意処理を組み込むことで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「広告SDKと同意処理を組み込む」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 同意状態に応じて配信を制御できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：同意状態に応じて配信を制御できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。

---

## M7-EX-02：毎局の結果後に全画面広告を実装する

- 領域：EX
- 依存TODO：M7-EX-01
- 目的：毎局の結果後に全画面広告を実装することで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「毎局の結果後に全画面広告を実装する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 対局中は表示せず、スキップ権があれば1回だけ自動消費される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：対局中は表示せず、スキップ権があれば1回だけ自動消費される。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。

---

## M7-EX-03：広告失敗・タイムアウト時の継続処理を作る

- 領域：EX
- 依存TODO：M7-EX-02
- 目的：広告失敗・タイムアウト時の継続処理を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「広告失敗・タイムアウト時の継続処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 結果保存と次操作を妨げない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：結果保存と次操作を妨げない。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。

---

## M7-EX-04：分析イベントとクラッシュレポートを組み込む

- 領域：EX
- 依存TODO：M7-SB-01
- 目的：分析イベントとクラッシュレポートを組み込むことで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「分析イベントとクラッシュレポートを組み込む」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 個人情報を過剰送信せず調査できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：個人情報を過剰送信せず調査できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-EX-05：利用規約、プライバシー、同意画面を作る

- 領域：EX
- 依存TODO：M7-PM-02
- 目的：利用規約、プライバシー、同意画面を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「利用規約、プライバシー、同意画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 初回・更新時の同意を記録できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：初回・更新時の同意を記録できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-EX-06：退会・データ削除導線を作る

- 領域：EX
- 依存TODO：M7-SB-03
- 目的：退会・データ削除導線を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「退会・データ削除導線を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 本人イベント削除と他参加者集計維持を明示し、誤操作防止付きで要求できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：本人イベント削除と他参加者集計維持を明示し、誤操作防止付きで要求できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 本人の対局イベントを削除し、他参加者には勝敗集計値のみ残す。削除前後の再計算と参照切れを確認する。

---

## M7-EX-07：メンテナンス・障害・更新必須画面を作る

- 領域：EX
- 依存TODO：M7-SB-04
- 目的：メンテナンス・障害・更新必須画面を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「メンテナンス・障害・更新必須画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 対局開始前に適切な案内が出る。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：対局開始前に適切な案内が出る。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-GR-01：同意、メンテナンス、通信障害の画面素材を作る

- 領域：GR
- 依存TODO：M7-EX-05～07
- 目的：同意、メンテナンス、通信障害の画面素材を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「同意、メンテナンス、通信障害の画面素材を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 世界観を保ちつつ内容を読める。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：世界観を保ちつつ内容を読める。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-GR-02：アプリアイコン、起動画面、ストア画像の初稿を作る

- 領域：GR
- 依存TODO：M6-GR-01～08
- 目的：アプリアイコン、起動画面、ストア画像の初稿を作ることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「アプリアイコン、起動画面、ストア画像の初稿を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- ストア要件に合わせた素材一覧が揃う。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：ストア要件に合わせた素材一覧が揃う。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-QA-01：広告成功・失敗・オフライン・同意拒否をテストする

- 領域：QA
- 依存TODO：M7-EX-01～03
- 目的：広告成功・失敗・オフライン・同意拒否をテストすることで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「広告成功・失敗・オフライン・同意拒否をテストする」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- いずれも対局結果を失わない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：いずれも対局結果を失わない。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 毎局終了後の広告と、所持上限1のスキップ権を前提とする。広告失敗で結果保存や次画面を阻害しない。

---

## M7-QA-02：分析イベントの欠落・重複・個人情報を確認する

- 領域：QA
- 依存TODO：M7-EX-04
- 目的：分析イベントの欠落・重複・個人情報を確認することで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「分析イベントの欠落・重複・個人情報を確認する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- イベント仕様どおりで過剰情報がない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：イベント仕様どおりで過剰情報がない。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M7-QA-03：退会・規約更新・メンテナンスをE2E確認する

- 領域：QA
- 依存TODO：M7-EX-05～07
- 目的：退会・規約更新・メンテナンスをE2E確認することで、広告・分析・運用の到達状態である「対局後広告、報酬広告、同意、計測、障害対応を安全に運用できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「退会・規約更新・メンテナンスをE2E確認する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 要求どおりアクセスが制御される。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：要求どおりアクセスが制御される。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 本人の対局イベントを削除し、他参加者には勝敗集計値のみ残す。削除前後の再計算と参照切れを確認する。
