# M1 詳細TODO：ローカル・ルールサンドボックス

- 文書ID：GAME-TODO-DETAIL-M1
- 版数：0.2
- 基準文書：`独自カードゲーム_横断マイルストーン_TODO_v0.1.md`、`独自カードゲーム_要件定義書_v0.1.md`
- TODO件数：18件

## このマイルストーンの到達状態

通信なしで任意局面の合法性と状態遷移を検証できる。

## 確定前提

- 確定済みの昼夜、追加、更新、属性ロック、追加封印、革命、Joker、上がり規則を唯一の基準とする。
- 最後の数字カード＋追加封印／革命は使用可能、最後の数字カード＋変化Jokerは使用不可とする。

## 共通実施ルール

- 依存TODOが未完了の場合は着手しない。並行可能な場合は、共有インターフェースだけ先に固定する。
- 表示名・日本語文言を内部IDや対局状態へ保存しない。安定IDと言語リソースを使用する。
- 非公開手札、未使用スキル、認証情報、個人情報をクライアントログや分析イベントへ出さない。
- Supabase変更はマイグレーション、Expo変更は自動テスト、グラフィック変更はアセット対応表を必須とする。
- 曖昧さや確定仕様との矛盾を発見した場合は作業を止め、要件の決定者へ確認する。

## TODO一覧

| TODO ID | 領域 | 概要 | 依存 |
|---|---|---|---|
| M1-SB-01 | SB | `ruleset_version`と有効ルール版の取得方法を作る | M0-SB-03 |
| M1-SB-02 | SB | マスタ整合性テストをSQLで作る | M0-SB-04 |
| M1-EX-01 | EX | `Card`、`SkillCard`、`PlayerState`、`RoundState`型を定義する | M0-EX-03 |
| M1-EX-02 | EX | 単体・同数セット・連番セットの解析器を作る | M1-EX-01 |
| M1-EX-03 | EX | 昼夜の強弱比較を作る | M1-EX-02 |
| M1-EX-04 | EX | 追加と更新の合法性判定を作る | M1-EX-02、M1-EX-03 |
| M1-EX-05 | EX | 属性ロックと追加封印を作る | M1-EX-04 |
| M1-EX-06 | EX | 自然革命と革命カードの処理を作る | M1-EX-04 |
| M1-EX-07 | EX | 場流しJokerと変化Jokerを作る | M1-EX-04 |
| M1-EX-08 | EX | パス、場流し、上がり、禁止上がりを作る | M1-EX-05～07 |
| M1-EX-09 | EX | プレイ全体を不変データから計算する関数を作る | M1-EX-02～08 |
| M1-EX-10 | EX | 任意状態を編集できるルールサンドボックス画面を作る | M1-EX-09 |
| M1-GR-01 | GR | 単体、同数、連番を見分ける仮レイアウトを作る | M0-GR-04 |
| M1-GR-02 | GR | 昼夜、属性ロック、追加封印、革命の仮アイコンを作る | M0-GR-03 |
| M1-GR-03 | GR | Joker宣言数字・属性のオーバーレイを作る | M0-GR-04 |
| M1-QA-01 | QA | 要件定義の必須ルールテストを自動化する | M1-EX-09 |
| M1-QA-02 | QA | ランダム入力で状態不変条件を検査する | M1-EX-09 |
| M1-QA-03 | QA | ルール検証チェックリストを作る | M1-QA-01 |

---

## M1-SB-01：`ruleset_version`と有効ルール版の取得方法を作る

- 領域：SB
- 依存TODO：M0-SB-03
- 目的：`ruleset_version`と有効ルール版の取得方法を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`ruleset_version`と有効ルール版の取得方法を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- アプリがルール版を表示できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：アプリがルール版を表示できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-SB-02：マスタ整合性テストをSQLで作る

- 領域：SB
- 依存TODO：M0-SB-04
- 目的：マスタ整合性テストをSQLで作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「マスタ整合性テストをSQLで作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. テーブル／関数／APIのスキーマを定義し、安定ID、主キー、外部キー、CHECK制約、一意制約、索引、時刻列を必要に応じて設定する。
3. 認証主体と読取・作成・更新・削除の権限を整理し、RLS、サーバー専用権限、冪等性、トランザクション境界を実装する。
4. 正常、未認証、権限外、重複、競合、再送、ロールバックのテストをマイグレーションと一緒に追加する。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- マイグレーション／サーバーコード
- 権限・スキーマ定義
- 自動テストと実行結果

### 完了条件

- 36枚・6枚・完全重複なしを検査できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：36枚・6枚・完全重複なしを検査できる。
- 空の検証環境へマイグレーションを適用し、正常系と権限外アクセスを自動実行する。
- 同じ要求の再送または競合を発生させ、重複更新や部分更新がないことを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-01：`Card`、`SkillCard`、`PlayerState`、`RoundState`型を定義する

- 領域：EX
- 依存TODO：M0-EX-03
- 目的：`Card`、`SkillCard`、`PlayerState`、`RoundState`型を定義することで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「`Card`、`SkillCard`、`PlayerState`、`RoundState`型を定義する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 表示名を含まない状態型が完成する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：表示名を含まない状態型が完成する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-02：単体・同数セット・連番セットの解析器を作る

- 領域：EX
- 依存TODO：M1-EX-01
- 目的：単体・同数セット・連番セットの解析器を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「単体・同数セット・連番セットの解析器を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 正常・境界・不正テストが成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：正常・境界・不正テストが成功する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-03：昼夜の強弱比較を作る

- 領域：EX
- 依存TODO：M1-EX-02
- 目的：昼夜の強弱比較を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「昼夜の強弱比較を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 昼夜の逆順テストが成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：昼夜の逆順テストが成功する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-04：追加と更新の合法性判定を作る

- 領域：EX
- 依存TODO：M1-EX-02、M1-EX-03
- 目的：追加と更新の合法性判定を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「追加と更新の合法性判定を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 複数枚追加と同枚数更新を判定できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：複数枚追加と同枚数更新を判定できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-05：属性ロックと追加封印を作る

- 領域：EX
- 依存TODO：M1-EX-04
- 目的：属性ロックと追加封印を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「属性ロックと追加封印を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 共存時を含むテストが成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：共存時を含むテストが成功する。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-06：自然革命と革命カードの処理を作る

- 領域：EX
- 依存TODO：M1-EX-04
- 目的：自然革命と革命カードの処理を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「自然革命と革命カードの処理を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 事前反転・自然革命・併用禁止を判定できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：事前反転・自然革命・併用禁止を判定できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-07：場流しJokerと変化Jokerを作る

- 領域：EX
- 依存TODO：M1-EX-04
- 目的：場流しJokerと変化Jokerを作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「場流しJokerと変化Jokerを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 宣言、完全重複、2枚Jokerを判定できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：宣言、完全重複、2枚Jokerを判定できる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-08：パス、場流し、上がり、禁止上がりを作る

- 領域：EX
- 依存TODO：M1-EX-05～07
- 目的：パス、場流し、上がり、禁止上がりを作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「パス、場流し、上がり、禁止上がりを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 最終出し手と勝者が正しく決まる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：最終出し手と勝者が正しく決まる。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-09：プレイ全体を不変データから計算する関数を作る

- 領域：EX
- 依存TODO：M1-EX-02～08
- 目的：プレイ全体を不変データから計算する関数を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「プレイ全体を不変データから計算する関数を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. ドメインロジックと表示・通信・永続化の責務を分離し、同じ入力に対して再現可能な結果を返すインターフェースを定義する。
3. 境界値、エラー、取消、再試行を実装し、途中状態や二重消費を残さない。
4. 単体テストと結合確認を追加し、ログには秘密情報・非公開手札・個人情報を含めない。
5. 実装または制作結果と、再現可能な確認手順を同じTODO内へ記録する。

### 成果物

- 実装コードまたは設定
- インターフェース仕様
- 単体・結合テスト

### 完了条件

- 不正時に入力状態が変化しない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：不正時に入力状態が変化しない。
- 正常・境界・失敗・再試行の代表ケースを自動または手順化して確認する。
- 別のクリーン環境または別端末で完了条件を再現する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-EX-10：任意状態を編集できるルールサンドボックス画面を作る

- 領域：EX
- 依存TODO：M1-EX-09
- 目的：任意状態を編集できるルールサンドボックス画面を作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「任意状態を編集できるルールサンドボックス画面を作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 画面の初期、読込中、操作可能、受付中、空、成功、入力不正、通信失敗、再試行の各状態を定義する。
3. 横画面のスマホ・タブレットでタップ領域、フォーカス順、文字拡大、色以外の識別、戻る操作を確認する。
4. 表示文言を言語キーへ分離し、画面ロジックから表示名や日本語文字列を直接参照しない。
5. 代表端末で操作動画またはスクリーンショットを残し、対応するUIテストを追加する。

### 成果物

- Expo画面・コンポーネント
- 画面状態一覧
- UIテストまたは実機確認記録

### 完了条件

- 任意ケースをUIから再現できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：任意ケースをUIから再現できる。
- Android 10以上のスマホ相当・タブレット相当を横画面で確認する。
- 通信失敗、再試行、文字拡大、TalkBack相当の読み上げ順を確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-GR-01：単体、同数、連番を見分ける仮レイアウトを作る

- 領域：GR
- 依存TODO：M0-GR-04
- 目的：単体、同数、連番を見分ける仮レイアウトを作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「単体、同数、連番を見分ける仮レイアウトを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 1～9枚を重なりなく確認できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：1～9枚を重なりなく確認できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-GR-02：昼夜、属性ロック、追加封印、革命の仮アイコンを作る

- 領域：GR
- 依存TODO：M0-GR-03
- 目的：昼夜、属性ロック、追加封印、革命の仮アイコンを作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「昼夜、属性ロック、追加封印、革命の仮アイコンを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 色なしでも状態を識別できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：色なしでも状態を識別できる。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-GR-03：Joker宣言数字・属性のオーバーレイを作る

- 領域：GR
- 依存TODO：M0-GR-04
- 目的：Joker宣言数字・属性のオーバーレイを作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「Joker宣言数字・属性のオーバーレイを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 使用場所、表示寸法、背景条件、状態差分、セーフエリア、透過、色空間、容量上限を制作仕様として固定する。
3. ラフ→レビュー→修正→承認→書出しの順で進め、色だけに依存せず意味を識別できる差分を用意する。
4. 編集可能な元データと最適化済み書出しを分離し、安定アセットID、版、命名規則、ライセンス情報を付ける。
5. 実機の最小表示と拡大表示で可読性、欠け、圧縮劣化、昼夜背景とのコントラストを確認する。

### 成果物

- 編集可能な元データ
- 最適化済みアセット
- アセット対応表・レビュー記録

### 完了条件

- 小さい表示でも宣言内容を読める。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：小さい表示でも宣言内容を読める。
- 最小表示、拡大表示、昼背景、夜背景、軽量設定で比較する。
- ファイル名、寸法、容量、透過、対応IDをアセット検査で確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-QA-01：要件定義の必須ルールテストを自動化する

- 領域：QA
- 依存TODO：M1-EX-09
- 目的：要件定義の必須ルールテストを自動化することで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「要件定義の必須ルールテストを自動化する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- T-RULE-001～022が成功する。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：T-RULE-001～022が成功する。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-QA-02：ランダム入力で状態不変条件を検査する

- 領域：QA
- 依存TODO：M1-EX-09
- 目的：ランダム入力で状態不変条件を検査することで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ランダム入力で状態不変条件を検査する」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- カード重複・枚数消失・二重消費が発生しない。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：カード重複・枚数消失・二重消費が発生しない。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。

---

## M1-QA-03：ルール検証チェックリストを作る

- 領域：QA
- 依存TODO：M1-QA-01
- 目的：ルール検証チェックリストを作ることで、ローカル・ルールサンドボックスの到達状態である「通信なしで任意局面の合法性と状態遷移を検証できる」を成立させる。
- 着手条件：依存TODOが完了し、必要な仕様・入力データ・確認環境へアクセスできること。

### 作業内容

1. 対象となる「ルール検証チェックリストを作る」の入力、出力、正常系、失敗系、対象外を要件IDと対応付ける。
2. 前提データと操作手順を固定したテストケースを作り、期待値と不合格条件を数値または観測可能な状態で記載する。
3. 正常系だけでなく、境界、再送、切断、競合、権限外、低性能端末、失敗からの復旧を対象に含める。
4. 失敗時に対局ID、状態版、要求ID、端末条件を記録し、同じ入力から再現できるようにする。
5. 結果をレポート化し、重大不具合0件と回帰テスト登録を確認して完了判定する。

### 成果物

- テストケース／自動化コード
- 実行レポート
- 不具合・回帰登録

### 完了条件

- 人手でも代表ケースを再現できる。
- 成果物がリポジトリまたはアセット管理表から追跡できる。
- 関連テストが成功し、重大度「高」の既知不具合が0件である。
- エラー時に部分更新、二重消費、情報漏洩、操作不能状態を残さない。

### 確認方法

- 受入確認：人手でも代表ケースを再現できる。
- 同じテストを連続実行して再現性を確認し、失敗ケースを保存する。
- 修正後に関連マイルストーンの回帰テストが成功することを確認する。

### 注意点・確定前提

- 確定仕様と矛盾が見つかった場合は実装を進めず、要件定義の該当IDへ未決事項として戻す。
